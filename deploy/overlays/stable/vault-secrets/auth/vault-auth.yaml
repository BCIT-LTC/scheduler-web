apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: vault-auth
spec:
  method: kubernetes
  mount: kubernetes-stable
  kubernetes:
    role: scheduler-web-kubeauthbot-stable
    serviceAccount: serviceaccount
    audiences:
      - vault
