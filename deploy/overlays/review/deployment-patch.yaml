apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
spec:
  selector:
    matchLabels:
      app: scheduler-web
  template:
    spec:
      containers:
        - name: scheduler-web
          volumeMounts:
            - name: keycloak-realm-cert
              mountPath: /app/saml/certs
              readOnly: true
      volumes:
      - name: keycloak-realm-cert
        secret:
          secretName: keycloak-realm-cert
