apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: scheduler-web-kubeauthbot-latest
spec:
  method: kubernetes
  mount: kubernetes-latest
  kubernetes:
    role: scheduler-web-kubeauthbot-latest
    serviceAccount: scheduler-web-serviceaccount
    audiences:
      - vault